{% extends 'front.html.twig' %}

{% block front %}
    <div class="row">
        <div class="col-12">
            <h3 class="mb-4">{{ guest.name }}</h3>
            <p class="mb-5 w-100">{{ guest.description }}</p>
            <div class="row">
                {% for media in guest.medias %}
                    <div class="col-4 media mb-4">
                        <img class="w-100" src="{{ media.path | imagine_filter('media_vignette') }}" alt="{{ media.title }}" loading="lazy">
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}

